@model IEnumerable<Grupp29.Models.ForumPostComment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @style = "font-size:larger;color: darkblue;" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Author)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CommentText)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ForumPostId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Author)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CommentText)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ForumPostId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }, new { @style = "font-size:larger;color: darkblue;" }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }, new { @style = "font-size:larger;color: darkblue;" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id }, new { @style = "font-size:larger;color: darkblue;" })
        </td>
    </tr>
}

</table>
